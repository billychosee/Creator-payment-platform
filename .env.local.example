# =============================================================================
# API PROVIDER CONFIGURATION
# =============================================================================
# Choose your backend API provider: localStorage, supabase, custom, or mock
# localStorage - Use browser localStorage (no setup required)
# supabase - Use Supabase as your backend (requires Supabase account)
# custom - Use your own API endpoint (requires API server)
# mock - Use mock API responses with simulated delays (for testing)

# RECOMMENDED: Use custom API for production deployments
NEXT_PUBLIC_API_PROVIDER=custom

# Enable/Disable API functionality
NEXT_PUBLIC_API_ENABLED=true

# =============================================================================
# CUSTOM API CONFIGURATION
# =============================================================================
# Required if using 'custom' as API provider
# Point to your own API endpoint (use the provided example server)

NEXT_PUBLIC_API_URL=http://localhost:3001/api
NEXT_PUBLIC_API_KEY=your-api-key-here

# =============================================================================
# SUPABASE CONFIGURATION (Alternative Option)
# =============================================================================
# Required if using 'supabase' as API provider
# Get these from your Supabase project settings: https://app.supabase.com/project/[your-project]/settings/api

# NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# =============================================================================
# FEATURE FLAGS
# =============================================================================
# Enable/disable specific features

NEXT_PUBLIC_ENABLE_PAYMENTS=true
NEXT_PUBLIC_ENABLE_PAYOUTS=true
NEXT_PUBLIC_ENABLE_PAYMENT_REQUESTS=true

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================
# Settings for development and debugging

NEXT_PUBLIC_DEBUG_MODE=false
NEXT_PUBLIC_LOG_API_CALLS=false

# =============================================================================
# PAYMENT PROCESSING (OPTIONAL)
# =============================================================================
# Add these when you're ready to integrate real payment processing

# Stripe (for payment processing)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
# STRIPE_SECRET_KEY=sk_test_...

# PayPal (for payment processing)
# NEXT_PUBLIC_PAYPAL_CLIENT_ID=your-paypal-client-id
# PAYPAL_CLIENT_SECRET=your-paypal-client-secret

# =============================================================================
# CUSTOM API SERVER SETUP
# =============================================================================
# 
# 1. CUSTOM API (Recommended for Production):
#    - Copy this file to .env.local
#    - Start the example API server:
#      cd server && npm install && npm run dev
#    - The server will run on http://localhost:3001
#    - Frontend connects to http://localhost:3001/api
#
# 2. ALTERNATIVE OPTIONS:
#    - localStorage: For development without server (NEXT_PUBLIC_API_PROVIDER=localStorage)
#    - Supabase: For managed database (NEXT_PUBLIC_API_PROVIDER=supabase)
#    - Mock: For testing API calls (NEXT_PUBLIC_API_PROVIDER=mock)
#
# 3. PRODUCTION DEPLOYMENT:
#    - Deploy the API server to your preferred platform
#    - Update NEXT_PUBLIC_API_URL to your production API endpoint
#    - Set proper JWT_SECRET and API_KEY
#
# Quick start:
# npm run dev (frontend)
# cd server && npm run dev (backend)

